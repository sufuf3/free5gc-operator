apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "free5gcservice.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "free5gcservice.name" . }}
    helm.sh/chart: {{ include "free5gcservice.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "free5gcservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
