apiVersion: v1
kind: Service
metadata:
  name: {{ include "free5gcservice.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "free5gcservice.name" . }}
    helm.sh/chart: {{ include "free5gcservice.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "free5gcservice.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
